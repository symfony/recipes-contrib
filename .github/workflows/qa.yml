name: Q&A

on:
    pull_request_target:

defaults:
    run:
        shell: bash

jobs:
    call-qa:
        uses: symfony/recipes/.github/workflows/callable-qa.yml@a0f880a116fad71183707149d747cbde9ec89593 # main (2026-07-28)
        with:
            contrib: true
            find_yaml: -not -wholename 'codeception/codeception/*/tests/*' -not -wholename 'codeception/codeception/*/codeception.yml'
        secrets:
            token: ${{ secrets.BOT_TOKEN }}
