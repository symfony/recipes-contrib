html_to_spreadsheet:
    # Activer le mode strict pour la validation HTML
    strict: true
    
    # Inclure les styles prédéfinis (th, money, date, etc.)
    include_builtins: true
    
    # Répertoire temporaire (optionnel)
    # temp_dir: '%kernel.cache_dir%/html_to_spreadsheet'
    
    # Styles personnalisés (exemples)
    # default_styles:
    #     custom_header:
    #         font:
    #             bold: true
    #             color: 'FFFFFF'
    #         fill:
    #             fillType: 'solid'
    #             color: '4472C4'