{
    "name": "acme/kiwicaptcha-symfony-recipe-fixture",
    "description": "Minimal fixture application the symfony/recipes-contrib validation installs the bel-consulting/kiwicaptcha-symfony recipe into (composer require installs the bundle, Flex applies the recipe: bundle registration, config, routes and .env placeholders).",
    "type": "project",
    "license": "MIT",
    "require": {
        "php": ">=8.1",
        "symfony/framework-bundle": "^6.4|^7.0|^8.0",
        "symfony/flex": "^2.4",
        "predis/predis": "^3.5",
        "bel-consulting/kiwicaptcha-symfony": "^1.0"
    },
    "extra": {
        "symfony": {
            "allow-contrib": true,
            "require": "^6.4|^7.0|^8.0"
        }
    }
}
